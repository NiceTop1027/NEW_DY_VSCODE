// Placeholder for Extension entity implementation

class Extension {
  constructor(id, version, publisher, supportedLanguages, supportedFeatures) {
    this.id = id;
    this.version = version;
    this.publisher = publisher;
    this.supportedLanguages = supportedLanguages; // Array of language IDs
    this.supportedFeatures = supportedFeatures; // Array of feature IDs
  }

  // Add methods for extension management here
}

module.exports = Extension;
