// Placeholder for Terminal Session entity implementation

class TerminalSession {
  constructor(shellType, cwd, pid) {
    this.shellType = shellType;
    this.cwd = cwd;
    this.pid = pid;
    this.output = [];
  }

  // Add methods for terminal interaction here
}

module.exports = TerminalSession;
