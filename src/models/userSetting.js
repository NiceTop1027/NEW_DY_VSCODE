// Placeholder for User Setting entity implementation

class UserSetting {
  constructor(key, value, scope) {
    this.key = key;
    this.value = value;
    this.scope = scope; // e.g., 'global', 'workspace'
  }

  // Add methods for user setting management here
}

module.exports = UserSetting;
