# 사용자별 독립 환경을 위한 Docker 이미지
FROM ubuntu:22.04

# 기본 패키지 설치
RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    nodejs \
    npm \
    gcc \
    g++ \
    make \
    git \
    curl \
    wget \
    vim \
    && rm -rf /var/lib/apt/lists/*

# 작업 디렉토리 생성
WORKDIR /workspace

# 기본 사용자 생성
RUN useradd -m -s /bin/bash codeuser && \
    chown -R codeuser:codeuser /workspace

# 사용자 전환
USER codeuser

# 기본 명령어
CMD ["/bin/bash"]
